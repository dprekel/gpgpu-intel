#include<stdio.h>

namespace NEO {

namespace WaitUtils {

void Init() {
	printf("Hey, we are in the rabbit hole!\n");
	//int32_t overrideWaitCount = DebugManager.flags.WaitLoopCount.get();
	//if (overrideWaitCount != -1) {
	//	waitCount = static_cast<uint32_t>(overrideWaitCount);
	//}
}

}

}
